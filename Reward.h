#include <stdio.h>
#include <iostream>
#include <string>
#include <cstdlib>

using namespace std
using std::cout;
using std::endl;
class Reward{
private:
	int ruby;
	//only constructor needed random generator of ruby, 
	//can create an array of all the ruby cards and remove from that array each time
	Ruby();
	//need a toString
}
